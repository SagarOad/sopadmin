interface Certificate {
  certificate_id: number;
  certificate_name: string;
  certificate_institutename: string;
  certificate_grade: string;
  certificate_year: string;
  certificate_obtainmarks: string;
}
